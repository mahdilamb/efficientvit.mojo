[build-system]
requires = ["setuptools", "wheel"]
[project]
authors = []
dependencies = [
  "torch==2.0.1",
  "torchvision==0.15.2",
  "opencv-python",
]
name = "efficient-vit-mojo"
requires-python = "~=3.10"
version = "0.0.1"
[tool.setuptools.package-dir]
efficient_vit = "."
